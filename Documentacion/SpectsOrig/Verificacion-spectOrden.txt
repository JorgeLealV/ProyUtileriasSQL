estoy creando una aplicación para que ejecuta archivos con instrucciones DDL y SQL contra una base de datos, hay archivos que en su nombre comienzan con la cadena "Ins_" y terminan con la cadena ".sql" si quitamos esos elementos nos queda un nombre de tabla en la base de datos lo que requiero es que se ejecuten los archivos Ins respetando la jerarquía de padre e hijo cuando así este especificado en la base de datos dejando pendientes los hijos y seguir con los archivos padres antes y luego proseguir con los hijos, esto lo voy a solicitar bajo el esquema de speckit en el aparatado especify con lo que contiene el archivo adjunto me podrías indicar si mi solicitud es correctamente lógica y que le faltaría 

